# Docker Compose environment
# Copy to .env in this directory (project root).
# Used by docker-compose for the Postgres container.

DB_PASSWORD=postgres
DB_DATABASE=omsimsocial
DB_USERNAME=postgres

# Host path for persistent uploads (avatars, covers, media).
# Example on Ubuntu: /opt/omsimsocial/storage
OMSIM_STORAGE_PATH=
